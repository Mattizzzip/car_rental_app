import 'package:flutter/material.dart';

class FavoriteVehicle extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return Container(
      decoration: BoxDecoration(
          color: Colors.blueGrey
      ),
      child: Text("FavoriteVehiclePage"),
    );
  }
}